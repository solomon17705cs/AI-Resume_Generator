{
  "name": "atsense-ai-resume-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm start",
    "lint": "cd web && npm run lint"
  },
  "description": "AI Resume Generator - Run from root or web folder",
  "dependencies": {
    "pdfmake": "^0.3.8"
  }
}
